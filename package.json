{
  "name": "team-control-panel",
  "version": "0.1.0",
  "private": true,
  "description": "Ежедневный пульт управления для проверки операционных метрик команды.",
  "type": "module",
  "scripts": {
    "start": "python3 -m http.server 4173",
    "test": "node --test tests/*.test.js",
    "check": "node --check src/main.js && node --check src/storage.js && node --check src/checklist.js"
  }
}
